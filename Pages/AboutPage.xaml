<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="KeyBoardTrainer.Pages.AboutPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KeyBoardTrainer.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="20">
        <StackPanel Spacing="20">
            <TextBlock Text="Статистика тренувань:" FontSize="24" FontWeight="Bold"/>

            <TextBox x:Name="ResultsTextBox"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     FontFamily="Consolas"
                     FontSize="16"
                     Height="400"
                     IsReadOnly="True"/>

            <Button Content="Оновити статистику"
                    Click="LoadResultsButton_Click"
                    Width="200"
                    HorizontalAlignment="Left"/>
        </StackPanel>
    </ScrollViewer>
</Page>